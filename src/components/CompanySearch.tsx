
import CompanySearch from './company/CompanySearch';
import { Company } from '@/types/company';

// Re-export the component and type for backward compatibility
export type { Company };
export default CompanySearch;
